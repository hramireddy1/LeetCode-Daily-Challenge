class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        n = len(nums)
        mid = sorted(nums) [n//2]
        res = sum(abs(mid-num) for num in nums)
        return res
            
        
